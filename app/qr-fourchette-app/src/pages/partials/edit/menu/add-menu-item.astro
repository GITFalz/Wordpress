---
import MenuItem from '../../../../components/MenuItem.astro';
const id = Astro.url.searchParams.get('id') || '';
const index = Astro.url.searchParams.get('index') || '';
---
<MenuItem index={index} name="Nouveau menu" description="Description du nouveau menu" entrees="Entrees du nouveau menu" plats="Plats du nouveau menu" desserts="Desserts du nouveau menu" id={id} />